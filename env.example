# Django settings
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (using default sqlite unless you change ENGINE in settings)
DATABASE_URL=sqlite:///db.sqlite3

# JWT lifetimes (match Slotify/settings.py)
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# Cache (defaults to locmem; set to Redis URI if needed)
CACHE_URL=locmem://
